/// Astrocyte Local — zero-infrastructure memory for AI coding agents.
///
/// Context Tree + SQLite FTS5 search. No database, no embeddings, no API keys.
///
/// TODO: Implement in Phase 3
/// - Context Tree: read/write markdown files with YAML frontmatter
/// - Search: SQLite FTS5 full-text search
/// - CLI: retain, search, browse, forget, export, import
/// - MCP server: stdio transport

fn main() {
    println!("astrocyte-local (Rust) — not yet implemented. Use the TypeScript or Python version.");
    std::process::exit(1);
}
